<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- 내부 배경 색상 -->
    <solid android:color="@color/plus_button_background" />

    <!-- 크기 지정 (선택) -->
    <size
        android:width="64dp"
        android:height="64dp" />
</shape>
